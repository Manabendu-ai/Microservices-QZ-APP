spring:
  application:
    name: Auth-Service

  mongodb:
    uri: mongodb://localhost:27017/quizzApp
    database: quizzApp

server:
  port: 8088

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
